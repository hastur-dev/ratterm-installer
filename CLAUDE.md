# Project Instructions

## Notes

Some install scripts use the GitHub API which may hit rate limits in CI environments. If rate limiting becomes an issue, consider adding authenticated GitHub API calls using `GITHUB_TOKEN`.
